# CLAUDE.md

## Режим работы с пользователем

**Важно: пользователь пишет этот проект сам.** Claude в этом репозитории выступает в роли технического консультанта/senior-инженера, к которому обращаются за советом — не как исполнитель, который сам пишет код и делает коммиты.

- По умолчанию **не пиши код и не редактируй файлы**, пока пользователь явно не попросит внести конкретное изменение.
- На вопросы вида "как лучше сделать X", "куда это положить", "правильно ли я делаю Y" — отвечай как консультация: рекомендация + обоснование + альтернативы/trade-off'ы, без немедленной реализации.
- Если пользователь просит именно написать/поправить код — это исключение из общего режима, делай это как обычно.
- Не бери на себя архитектурные решения самостоятельно — предлагай варианты, финальный выбор за пользователем.

## О проекте

**QuizApp** — платформа для создания и прохождения викторин с AI-генерацией вопросов (Claude API), соло и мультиплеер-режимами, соцфункциями (подписки, лайки, челленджи) и монетизацией.

Полный план — в [`quiz_project_plan.md`](quiz_project_plan.md): стек, структура папок, модели, сценарии использования, этапы разработки.

### Стек
Django 5 (sync) + PostgreSQL + Redis + Celery + Django Channels (WebSocket) + HTMX + Anthropic Claude API. Деплой — Hetzner/DigitalOcean (Frankfurt), Gunicorn + Nginx.

### Ключевое архитектурное правило
Приложение `ai_generator` зависит от `quizzes` (импортирует его модели, чтобы сохранять сгенерированные викторины), но не наоборот. `quizzes` — чистое хранилище/отображение (`Quiz`, `Question`, `AnswerOption`), не знает, создана ли викторина вручную или через AI. Форма параметров AI-генерации (тема, кол-во вопросов, сложность, стиль) размещается в `ai_generator`, а не в `quizzes`. Подробности — в разделе "Правило взаимодействия quizzes ↔ ai_generator" в плане.

### Текущее состояние (на момент последнего обновления этого файла — 2026-09-18)

- Django-проект `quiz_project` инициализирован: `settings.py`, `urls.py`, `asgi.py`, `wsgi.py` (пока единым файлом, план предполагает разбивку на `settings/base|development|production.py`). Фактически установлен Django 6.0.7 (план/этот файл раньше упоминали Django 5 — не критично, но при апгрейде плана стоит поправить).
- Создано приложение `users`: кастомная модель `User(AbstractUser)` (`email` unique, `ai_quizzes_generated`, `ai_quizzes_limit_per_day`, `is_subscribed`, `is_premium`, `email_confirmed`, `subscription_expires_at`). `AUTH_USER_MODEL = 'users.User'`. Важно: кастомная модель пользователя должна существовать до первого `migrate` в проекте — если БД уже проинициализирована со стандартным `auth.User`, при подключении кастомной модели ловите `InconsistentMigrationHistory` (уже наступали на это, чинится пересозданием БД на dev-окружении).
- Приложение `quizzes` — модели реализованы (не заглушки), схема с 2026-09-12 двухуровневая: `Category` (`title`), `QuizSeries` (то, что пользователь в UI называет "квиз" — `title`, `user`, `description`, `category` FK `on_delete=PROTECT`, `status` — private/public, default `private`, `created_at`), `Quiz` (по сути РАУНД внутри серии — историческое имя не переименовано, см. `quizzes/models.py` докстринг; `user`, `series` FK на `QuizSeries` nullable, `round_order`, `type` — ai/by_user, `subject`, `level`, `style`, `audience`, `time_limit_seconds`, `created_at` — **без** `title`/`description`/`category`/`status`, они теперь только на `QuizSeries`), `Question` (`quiz`, `text`, `order`, `fact`), `AnswerOption` (`question`, `text`, `is_correct`, `order`).
- Приложение `ai_generator` реализовано: `GenerationRequest` (параметры генерации + `category` FK + `result` JSONField + `status`), `GenerationRequestForm`, `QuestionForm`/`QuestionFormSet` (formset для редактирования вопросов перед сохранением), `prompts.py` (`generate_quiz_questions()` — запрос к Claude через HTTP-прокси), `views.index()` (генерация + рендер формы на правку) и `views.save()` (сохранение через `quizzes.services.create_quiz_from_any_data`).
- Реализован flow AI-генерации: пользователь заполняет параметры → `index()` синхронно (без Celery — отличие от сценария в плане, Celery ещё не подключён) вызывает Claude → результат сохраняется в `GenerationRequest.result` и в БД создаётся сама `GenerationRequest` → пользователь получает `formset` с вопросами на `temp_ai_quiz.html`, редактирует → сабмит на `save()` → `quizzes/services.py` создаёт `Quiz`/`Question`/`AnswerOption`.
- Большинство ранних блокирующих багов (опечатки `object`/`objects`, синтаксическая ошибка в `services.py`, отсутствующий импорт, ленивый `get_all_categories` и т.д.) уже закрыто — актуальный статус по каждому пункту (закрыт/открыт) смотрите в [`TODO.md`](TODO.md), не полагайтесь на память об этом списке, он обновляется по ходу ревью.
- Добавлена авторизация: приложение `users` получило `urls.py` (`login/`, `logout/`, `app_name = "users"`, подключено в корневой `urls.py` под `/users/`), `RateLimitedLoginView` (наследник `auth_views.LoginView`, лимит неудачных попыток входа по IP через кэш, окно — 1 час, `timeout=3600`) и шаблоны `users/base.html`, `users/login.html`.
- Добавлена регистрация: `users/forms.py` → `RegisterForm(UserCreationForm)` — переопределяет `Meta.model` на кастомный `users.models.User` (у родителя `Meta.model` указывает на стандартный `django.contrib.auth.models.User`, без переопределения форма создавала бы не те инстансы), добавляет `email` (с проверкой уникальности и блокировкой одноразовых доменов через `clean_email`), `privacy_policy` (обязательный чекбокс) и `website` — honeypot-поле (`clean_website` отклоняет форму, если оно заполнено). `users/views.py` → `register()` (rate-limit по IP через тот же `CACHES`, вход сразу после регистрации, письмо админу на `SUPPORT_EMAIL` и письмо-подтверждение email через `django.core.signing`, TTL 24 часа) и `confirm_email()` (валидирует подписанный токен, выставляет `email_confirmed=True`). Есть также `privacy_policy()`/`public_offer()` — статичные страницы (тексты сейчас скопированы из другого проекта пользователя, "PrepStats" — под замену). Шаблон `register.html` — honeypot спрятан визуально через inline-style (не только `aria-hidden`), кнопка сабмита разблокируется JS по чекбоксу `privacy_policy` (прогрессивное улучшение, реальная проверка — на бэкенде).
- **Регистрация ещё не проверена end-to-end через реальный браузер** — только через `manage.py shell` + тестовый `Client()`, что по ходу дела засорило файловый кэш (`CACHES` шарится между `runserver` и shell/test-client, оба резолвят IP в `127.0.0.1`) и заблокировало реальную первую попытку пользователя rate-limit'ом. Подробности и остальные находки (битые `{% url %}` в `public_offer.html`, асимметрия rate-limit счётчиков логина/регистрации, тестовый юзер `testuser3` в БД, `User` не зарегистрирован в `users/admin.py`) — в [`TODO.md`](TODO.md).
- `ai_generator.views.index/save` и `quizzes.views.QuizzesDetailView` закрыты авторизацией (`@login_required` / `LoginRequiredMixin`). `LOGIN_URL = '/users/login/'`, `LOGIN_REDIRECT_URL = '/quizzes/menu/'`, `LOGOUT_REDIRECT_URL = '/users/login/'`. Базовые шаблоны `ai_generator/base.html` и `quizzes/base.html` получили безусловную форму "Выйти" (без проверки `user.is_authenticated` — см. TODO). Корневой `quiz_project/urls.py` — добавлен `path('', RedirectView.as_view(pattern_name='quizzes:menu', permanent=False))`, редиректит на меню квизов (анонимных дальше перехватывает `login_required` → `/users/login/`).
- Для rate-limit login/register добавлен `CACHES` (`FileBasedCache` в `/tmp/django_cache_quiz_project`) — раньше кэш в проекте не был настроен. **Важно для будущих сессий: этот кэш — файлы на диске, общие для любого процесса, читающего те же настройки** (`runserver`, `manage.py shell`, тестовый `Client()`) — при ручной проверке rate-limited эндпоинтов через shell велик шанс случайно засорить состояние для параллельно работающего dev-сервера пользователя (уже наступали на это с регистрацией, см. выше). Перед такими проверками стоит либо предупреждать пользователя, либо чистить использованные кэш-ключи после теста.
- Обучающий материал по формам Django (на примере `register.html`) — в [`FORMS_LEARNING.md`](FORMS_LEARNING.md), написан для пользователя, который сам изучает Django. Дополнен разбором JS-фрагмента (`register.html`, чекбокс `privacy_policy` → разблокировка кнопки сабмита) — отдельно зафиксировано частое заблуждение "`addEventListener` — это цикл" (это не так, это однократная регистрация обработчика события) и уточнение про начальное состояние чекбокса (не "default у `BooleanField`", а рендер unbound-формы на GET).
- `quizzes` получил рабочие views/urls/шаблоны: `views.menu()` (страница-меню на `/quizzes/menu/`, шаблон `quizzes_menu.html`), `QuizzesListView` (список **своих** квизов — `get_queryset()` фильтрует `Quiz.objects.prefetch_related("questions__options").filter(user=self.request.user)`; изначально было по ошибке написано как класс-атрибут `queryset = ...filter(user=self.request.user)`, что падало `NameError`, т.к. `self` не существует на уровне тела класса — исправлено на метод), `QuizzesDetailView` (без изменений). Шаблоны переименованы под конвенцию Django CBV (`quiz_list.html`, `quiz_detail.html` — `<model_name>_list/detail.html`), старый `quizzes_index.html` удалён. `quizzes/urls.py` был со случайной двойной вложенностью (`path('quizzes/', ...)` внутри приложения, уже смонтированного в корне под `/quizzes/`) — из-за этого список викторин фактически висел на `/quizzes/quizzes/`, а `/quizzes/` доставался пустой странице-заглушке; исправлено (`menu/`, `<int:pk>/`, `''`).
- Несколько шаблонных багов с `{% url %}` исправлено: голые строки `href="users:login"` без тега `{% url %}` (браузер трактовал `users:` как кастомную URL-схему и предлагал открыть через system handler), и битый вызов `{% url quizzes:quizzes_list' %}` (потеряна открывающая кавычка у имени маршрута, `TemplateSyntaxError`). Также была найдена и исправлена опечатка в `settings.py`: `LOGIN_REDIRECT_URL = 'quizzes/menu/'` без ведущего слэша — Django отдавал это как относительный `Location`, браузер клеил его к текущему пути (`/users/login/` + `quizzes/menu/` → `/users/login/quizzes/menu/`, 404). Сейчас `LOGIN_REDIRECT_URL = '/quizzes/menu/'`, корневой redirect (`quiz_project/urls.py`) — на `quizzes:menu`.
- `RateLimitedLoginView` получил `redirect_authenticated_user = True` — раньше уже залогиненный пользователь, зайдя на `/users/login/`, видел форму логина заново (стандартное поведение Django `LoginView`, флаг по умолчанию `False`); теперь его редиректит на `LOGIN_REDIRECT_URL`.
- `users/admin.py` — `User` зарегистрирован в админке (`UserAdmin` с `list_display = pk/username/first_name/last_name/is_superuser/email/email_confirmed`, `ordering=("pk",)`). Пока не хватает остальных кастомных полей (`is_premium`, `is_subscribed`, `ai_quizzes_generated`, `ai_quizzes_limit_per_day`, `subscription_expires_at`) в `list_display`/`fieldsets` — см. TODO.
- В `Quiz` добавлено поле `audience` (`CharField`, `max_length=20`, `choices=AUDIENCE_CHOICES`, `default='common'`, миграция `0004_quiz_audience.py`) — раньше аудитория задавалась только на этапе AI-генерации (`GenerationRequest.audience`) и никак не попадала в сам `Quiz`. `quizzes/services.py::create_quiz_from_any_data` обновлён — теперь пробрасывает `audience=gen_request.audience` при создании `Quiz` (раньше поле молча получало бы дефолт модели `'common'` независимо от того, что реально выбрал пользователь в форме генерации).
- Реализовано **ручное создание квиза** (`quizzes/forms.py`, `quizzes/views.py::QuizzesCreateView`, шаблон `quiz_form.html`, маршрут `quizzes:quizzes_create` → `/quizzes/create/`, ссылка добавлена на `quizzes_menu.html`):
  - `QuizForm` (`ModelForm` на `Quiz`) — `Meta.fields` не включает `user`/`type` (проставляются во view: `user` — чтобы нельзя было создать квиз "от чужого имени" через список всех пользователей в форме, `type='by_user'` — чтобы отличать от AI-квизов) и `created_at` (`non-editable`-поле, `ModelForm` не даёт включить его в `fields` — падает `FieldError`). Кастомные подписи — через `Meta.labels`, виджет `description` — через `Meta.widgets`; поля `category`/`level`/`status`/`style`/`audience` **не переопределены** явно на классе — `ModelForm` сама генерирует нужный тип поля (`ModelChoiceField`/`ChoiceField`) прямо из `choices`/`FK` модели, один источник правды вместо дублирования списков choices в форме.
  - `QuestionForm` (`ModelForm` на `Question`, `fields=["text","order","fact"]`) + 4 плоских поля `option_1`..`option_4`/`correct_index` — не входят в `Meta.fields` (`Question` их не хранит как атрибуты), объявлены явно на классе, по аналогии с `ai_generator/forms.py::QuestionForm` (сознательный выбор — фиксированное число вариантов ответа вместо вложенных формсетов на `AnswerOption`, второй уровень вложенности Django из коробки не поддерживает).
  - `QuestionFormSet = forms.inlineformset_factory(Quiz, Question, form=QuestionForm, extra=1, can_delete=True)`. Связь с `QuizForm` не декларативная — только процедурная, во view: `question_formset.instance = self.object` подменяет пустую несохранённую `Quiz()`-заглушку (её `inlineformset_factory` подставляет сам, если `instance=` не передан при создании) на реально сохранённый объект **до** `question_formset.save()` — иначе `Question.quiz` попытался бы сохраниться со ссылкой на объект без `pk`.
  - `QuizzesCreateView(LoginRequiredMixin, CreateView)` совмещает `QuizForm` + `QuestionFormSet` на одной странице — стандартные `form_valid`/`form_invalid` рассчитаны только на одну форму, поэтому `post()` переопределён целиком (валидирует обе формы разом), а сохранение вынесено в `forms_valid()` (`@transaction.atomic`): `form.save()` → `question_formset.instance = self.object` → `question_formset.save()` → вручную создаёт `AnswerOption` из `question_form.cleaned_data['option_1'..'4']`/`['correct_index']` для каждого сохранённого вопроса — `ModelForm.save()`/`formset.save()` переносят в БД только поля, перечисленные в `Meta.fields`, `option_1..4`/`correct_index` там нет, так что без этого шага `formset.save()` молча создал бы вопросы без единого варианта ответа.
  - Шаблон `quiz_form.html` рендерит `{{ question_formset.management_form }}` + цикл по формам (`forloop.counter` для нумерации "Вопрос №N"), обёрнутым в контейнер `#question-list`.
  - `quiz_detail.html` дополнен отображением `object.get_audience_display` и нумерацией вопросов через `forloop.counter`.
- Реализовано **динамическое добавление/удаление вопросов** в `quiz_form.html` без перезагрузки страницы (чистый клиентский JS, без новых view/эндпоинтов). Среди трёх обсуждённых вариантов (JS-клонирование `empty_form`, HTMX-эндпоинт за новой формой, статичный `extra=N`) выбран первый — не требует заводить сервер-сайд ради генерации одной формы. Механика: скрытый `<template id="empty-question-template">` с `{{ question_formset.empty_form }}` (сам `<template>` браузер не рендерит в видимый DOM) служит заготовкой — кнопка "Добавить ещё вопрос" клонирует его HTML, заменяет плейсхолдер `__prefix__` (который Django подставляет в `name`/`id` полей `empty_form`) на текущее значение `TOTAL_FORMS` и инкрементирует это скрытое поле. Кнопка "Удалить вопрос" не убирает форму из DOM, а ставит формсетный `DELETE`-чекбокс в `true` и прячет блок через `display:none` — так индексация (`questions-0-`, `questions-1-`...) оставшихся форм не сбивается. Селекторы в JS — по суффиксу (`input[name$="-TOTAL_FORMS"]`, `input[name$="-DELETE"]`), без хардкода конкретного prefix формсета (`questions-`, определяется `related_name` FK `Question.quiz`, у `inlineformset_factory` его никто явно не задавал). **Не протестировано в реальном браузере** — см. TODO.
- По ходу той же сессии разобран сам механизм `question_formset.management_form`/`TOTAL_FORMS` по исходникам Django (`django/forms/formsets.py`, `BaseFormSet`): `total_form_count()` для bound-формсета возвращает значение `TOTAL_FORMS` из `request.POST` (с потолком `absolute_max` — защита от DoS, если кто-то пришлёт заведомо большое число), `forms` (`cached_property`) строит список подформ циклом `range(total_form_count())`, каждая подформа получает `prefix=f"{formset.prefix}-{i}"` и сама выбирает свои поля из общего `request.POST`. `INITIAL_FORMS` — отдельная величина, определяющая, какие индексы формсет считает формами "существующих" объектов (`i < INITIAL_FORMS`, `save_existing_objects()`) против "новых" (`i >= INITIAL_FORMS`, `save_new_objects()`, `empty_permitted=True`, пустые/непомеченные формы пропускаются); в `QuizzesCreateView` формсет всегда создаётся без готового `queryset`, поэтому `INITIAL_FORMS=0` и все формы идут по ветке "новый объект". Отсюда прямое следствие для JS выше: без инкремента `TOTAL_FORMS` сервер просто никогда не дойдёт до чтения полей склонированной формы в этом цикле — данные останутся в `request.POST`, но `_construct_form()` для их индекса не вызовется.
- **В проекте до сих пор нет ни одного CSS-файла** — вся вёрстка держится на дефолтных user-agent стилях браузера (отсюда, например, эффект "форма выхода и `div` с именем стоят друг под другом, а два `<a>` подряд идут в один ряд" — блочные vs строчные элементы по умолчанию). Базовые стили — следующий запланированный шаг, см. [`TODO.md`](TODO.md).
- **Этап 4 (раунды/серии, ветка `rounds_creating`, начат 2026-09-12, доведён до рабочего состояния 2026-09-13)**: `Quiz` переосмыслен как раунд внутри `QuizSeries` (см. модели выше) без переименования исторического имени класса. Ручное создание/редактирование раунда (`quizzes/forms.py::QuizForm`/`QuizFormWithSeriesId` + `QuizCreateView.get_form_class()`/`RoundUpdateView`) и AI-генерация (`ai_generator/forms.py::GenerationRequestForm`/`GenerationRequestFormWithSeries_id` + `views.py::index()`/`save()` → `quizzes/services.py::create_quiz_from_any_data`) — оба флоу переведены на новую схему и рабочие (миграции `ai_generator/0002_...`/`quizzes/0008_...` применены, серия багов "поле перенесено с Quiz на QuizSeries, но не везде обновлено" закрыта — подробности сессий от 2026-09-12/13 в [`CLAUDE_HISTORY.md`](CLAUDE_HISTORY.md) и [`TODO.md`](TODO.md)).
- **Первая попытка флоу «пройти серию раундов подряд» — соло, начата тем же днём 2026-09-13 (`gameplay/views.py::start`/`solo_room`/`_play_solo`)**: `start(pk)` (`pk` серии, URL остался старый `/gameplay/quiz/<pk>/start/`) создаёт `SeriesRun` и ведёт на `solo_room(pk)` (`pk` — `SeriesRun`) — промежуточный экран со списком уже завершённых раундов серии и кнопкой "начать следующий", которая создаёт `GameSession` текущего раунда (`series_run.series.rounds.filter(round_order=series_run.current_round_index).first()`) и ведёт на уже существующий `gameplay:play`. `_play_solo` при завершении раунда дополнительно продвигает `session.series_run.current_round_index` (или выставляет `None`, если раундов больше не осталось). По ходу реализации закрыто несколько багов (порядок полей `QuizForm` через `field_order`, `NoReverseMatch` в `round_confirm_delete.html`, `ValueError` из-за забытого `.first()` в `solo_room()`, шаблонный `completed_session.all` на обычном списке) — подробности в [`TODO.md`](TODO.md) и [`CLAUDE_HISTORY.md`](CLAUDE_HISTORY.md). **Ключевое — обновление `current_round_index` в `_play_solo` пока не подтверждено живым прохождением**: единственный завершённый на сейчас тестовый раунд (`GameSession №13`) был пройден раньше, чем этот код появился/стал активным, так что реального сквозного теста ещё не было (сама логика прогнана вручную в shell и отработала корректно, но это не замена реальному прохождению). **Не реализовано**: `result()` не знает про `series_run` (нет кнопки "следующий раунд"/сводного итога серии — переход между раундами пока только через `solo_room`), мультиплеерная ветка (`room_start`/`_check_and_make_complete` через `Room.current_series_run`), UI сборки серии из уже существующих раундов (раунды по-прежнему присоединяются к серии только по ссылке с `series_id` в URL). Чек-лист — `quiz_project_plan.md`, раздел «Раунды (Этап 4)».
- **Продолжение соло-флоу, сессия от 2026-09-14 (`gameplay/views.py`, `result.html`, `solo_room.html`)**: пользователь правил код сам, Claude — только ревью каждого захода. Закрыты находки предыдущей сессии и несколько новых: **IDOR в `solo_room()`** — вьюха не проверяла `series_run.created_by == request.user` (чужой `pk` в URL давал доступ на чтение чужого прогресса серии и, через POST, позволял создать `GameSession` внутри чужого `SeriesRun`, продвигая его состояние) — добавлена проверка владения в начале вьюхи, до ветвления GET/POST. **`start()` GET-ветка искала активный раунд без фильтра по пользователю** (`for run in series.runs.all(): for game_session in run.game_sessions.all(): ...`) — при параллельном прохождении публичной серии несколькими пользователями редиректило на чужую `GameSession` (дальше подчищалось `PermissionDenied` в `play()`, но UX был сломан) — исправлено на прямой `GameSession.objects.filter(series_run__series=series, created_by=request.user, status="in_progress")`. **Продвижение раунда переведено на устойчивую к дыркам в `round_order` схему**: новая `_advance_series_run(series_run, completed_round_order)` ищет следующий раунд через `series.rounds.filter(round_order__gt=completed_round_order).order_by("round_order").first()` (по аналогии с тем, как `_check_and_advance_question`, бывшая `_check_and_advance_round`, уже искала следующий вопрос) вместо старой арифметики `current_round_index + 1` vs `rounds.count()` — та ломалась, если раунд удалён из середины серии через `RoundDeleteView` (`round_order` не переиндексируется при удалении). По пути пойман баг первой версии фикса — в `start()` при создании `SeriesRun` в `current_round_index` присваивался сам объект `Quiz` (`series.rounds.order_by('round_order').first()`) вместо его `round_order`, `PositiveIntegerField` не может сохранить инстанс модели — исправлено на `first_round.round_order if first_round else None`. **`result.html`** дополнен постатейным разбором ответов участника (`{% for answer in curr_participant.participants_answers.all %}`, `{{ answer.question.text|truncatechars:40 }}`, `✅ верно`/`❌ пропущено`/`❌ неверно`) — по пути поймана опечатка `{% elseif %}` (в Django template language нет такого тега, только `{% elif %}`, `TemplateSyntaxError`). **`solo_room()`**: добавлен подсчёт `total_score` по всем завершённым раундам серии и `is_completed` в контексте (шаблон скрывает форму "начать следующий раунд", показывает итог) — первая версия считала `sess.participants.first().score`, что незаметно сводило на нет уже добавленный `prefetch_related("game_sessions__participants")` (`QuerySet.first()` без `Meta.ordering` на модели клонирует queryset через `order_by('pk')`, а клон не наследует `_result_cache` prefetch'а — лишний запрос на каждую сессию), исправлено на `sess.participants.all()[0]` (слайсинг читает из уже загруженного кэша). **Всё ещё не проверено вживую в браузере** — только сквозное вычитывание кода и ревью каждого диффа; реальное прохождение серии из нескольких раундов до конца с этой версией кода не проводилось.
- **Мультиплеер переведён на раундовую архитектуру (Этап 4, ветка `rounds_creating`, сессии от 2026-09-15/16, план — `temp_plan_multiplayer.md`)**: хост теперь выбирает `QuizSeries` вместо одиночного `Quiz`. `multiplayer.Room` — `current_quiz` удалён (миграция `0005_remove_room_current_quiz_room_current_series.py`), взамен `current_series` (FK `QuizSeries`, что выбрано) + переосмысленный `current_series_run` (FK `SeriesRun`, что реально идёт — создаётся только в `room_start`, в момент старта первого раунда серии, по аналогии с уже существовавшей парой `current_quiz`/`current_game_session`). `room_set_quiz`/`room_reset_quiz`/`RoomQuizForm` заменены на `room_select_series`/`room_reset_series`/`RoomSeriesForm` (`multiplayer/forms.py`, `multiplayer/views.py`) — **имена URL-маршрутов и сами вьюхи-обработчики в `urls.py` не переименованы** (`path('.../set-quiz', views.room_select_series, name='room_set_quiz')` — старые имена намеренно оставлены как есть, это отдельный пункт Шага 10 плана, не сделан). Новый общий хелпер **`gameplay/services.py::get_series_progress(series_run)`** — единая форма прогресса серии (завершённые раунды, текущий раунд, лидерборд через `values("user_id").annotate(Sum("score"))`+`in_bulk`, `is_completed`) для solo и multiplayer, ветвление по `mode` только внутри функции; переиспользован и в `gameplay/views.py::solo_room()` (заменил самодельный подсчёт `total_score`), и в `multiplayer/views.py::_get_room_context()`. Новый partial `gameplay/templates/gameplay/_series_progress.html` включается и в `solo_room.html`, и в `multiplayer/_room_status.html` (передаётся вместе с остальным WS-обновляемым статусом). WS-payload (`multiplayer/consumers.py::RoomConsumer.room_update`) расширен флагами `has_selected_series`/`can_start_round`/`is_first_round`, `room_detail.html` переключает по ним видимость трёх статичных форм (выбор серии / сброс / начать раунд) и подпись кнопки.
  - **Реализация — целиком консультационная роль Claude**: пользователь писал весь код сам (форма/модель/вьюхи/шаблоны/миграция), Claude — только ревью каждого захода (4 итерации подряд) + консультация по обработке гонки двойного клика на создании `SeriesRun`/`GameSession`. По ходу ревью закрыта цепочка блокеров (каждый пойман до того, как пользователь тестировал вживую в браузере, чисто чтением диффа): обращение к уже удалённому `room.current_quiz` в `room_start`; опечатка `.selected_related(` вместо `.select_related(` в консьюмере (рвала WS на каждом апдейте); ключи контекста с лишними пробелами (`"can_start_round  "`) → `KeyError` в консьюмере; `Room.current_game_session` никогда не сбрасывался обратно в `None` после завершения раунда → кнопка «начать следующий раунд» пропадала навсегда после первого же раунда (добавлен сброс в `gameplay/views.py::_check_and_make_complete`, рядом с уже существовавшим сбросом `status="waiting"`); несовпадение имени ключа в `_room_status.html` (`{% if series_progress %}` при плоском `context.update(get_series_progress(...))`) — заменено на `{% if series_run %}`. Вторая волна (уже при попытке закрыть смену/сброс активной серии, п. Шаг 5 плана про `unique_in_progress_session_run_per_user_series`): **`.update()` вызван на инстансах моделей вместо `.save()`** (`room.current_series_run.update(...)`, `room.update(...)` в `room_select_series`/`room_reset_series`) — `AttributeError`, т.к. `.update()` существует только у `QuerySet`/related-менеджера (обратный accessor `room.room_players` — обратная связь от чужого `RoomPlayer.room`, отсюда путаница), а не у объекта, полученного через прямой `ForeignKey`-дескриптор (`room.current_series_run`, `room` сам); исправлено на `instance.field = ...; instance.save(update_fields=[...])`. Следом — `current_round` в `room_start` вычислялся **до** проверки `current_series_run is None`, из-за чего `None.current_round_index` ронял `AttributeError` на самом первом старте новой серии (самый частый путь) — вычисление перенесено внутрь `transaction.atomic()`, после ветки создания `SeriesRun`; заодно разведены на два независимых `try/except IntegrityError` два разных unique-constraint'а (`SeriesRun` на `("series","created_by")` и `GameSession` на `("quiz","created_by")` при `status="in_progress")` — раньше один `except` пытался востановить сессию по неверным полям после гонки на создании `SeriesRun`. Отдельно — ключ `is_first_round` не выставлялся в контексте вовсе, когда `current_series_run` уже существовал (`get_series_progress()` такого ключа не возвращает) → `KeyError` в консьюмере на каждом апдейте после первого раунда; исправлено выносом `context["is_first_round"] = room.current_series_run is None` из-под `if/else` наружу, выполняется всегда. И `solo_room.html` — ссылка «Вернуться в комнату» для мультиплеерных прогонов передавала `code=series_run.room_id` (numeric FK id) вместо `series_run.room.token` (`multiplayer:room_detail` ждёт именно `token`) — вела на несуществующую комнату. Финальный штрих — добавлен guard на `current_round is None` (раунд удалён из середины активной серии между продвижением и стартом) прямо в `room_start`, чтобы такой `IntegrityError` (`NOT NULL` на `quiz_id`, не unique-constraint) не путался с обработкой двойного клика и не падал `DoesNotExist` в фолбэке.
  - **`manage.py check` чистый.** Открыто по Шагу 10 плана (уборка, не блокеры): переименование URL-имён/маршрутов `room_set_quiz`→`room_select_series` и т.п. в `multiplayer/urls.py`; `room_select_series` — `form.save()` без `update_fields` (риск затереть параллельно изменённые `status`/`current_game_session` полным сохранением инстанса); мёртвые импорты в `multiplayer/views.py` (`RoomPlayerReadyForm`, `render`, `HttpResponse`, `reverse_lazy`, `UpdateView`); опечатка «готовноти» в `messages.success`.
- **Сессия от 2026-09-17 — первый живой сквозной тест мультиплеера в браузере, Шаг 9 плана `temp_plan_multiplayer.md` закрыт.** Пользователь прошёл мультиплеер целиком (несколько раундов подряд, завершение серии), крупных проблем не нашёл; найденное по ходу исправил сам, Claude — ревью диффа постфактум. Главная находка (уже обсуждалась отдельно в этой же сессии до правки): `gameplay/services.py::get_series_progress` считал прогон завершённым только при `status == "completed"`, не учитывая третий статус `SeriesRun.status` — `"abandoned"` (проставляется в `multiplayer/views.py::room_select_series`/`room_reset_series`, когда хост меняет/сбрасывает серию до того, как она доиграна) — брошенный прогон, открытый напрямую, показывал кнопку «начать следующий раунд», хотя продолжать его не должно было быть можно; исправлено на `status != "in_progress"`. Заодно: блок «История игр» в `room_detail.html` теперь показывает только реально завершённые/брошенные прогоны (новый `completed_series_run` в `_get_room_context`, со статусом и `finished_at` в подписи) вместо всех `Room.series_runs.all()` без фильтра — это заодно закрывает риск открыть через эту ссылку ещё не завершённый мультиплеерный прогон и продвинуть его в обход `room_start`/WS; `_advance_series_run` теперь проставляет `finished_at` при естественном завершении серии (раньше — только в `abandoned`-ветке); `_check_and_make_complete` при завершении последней раунда мультиплеерной серии сбрасывает `room.current_series`/`current_series_run` в `None` (раньше сбрасывались только `current_game_session`/`status`/готовность игроков). Подробности и остаточные пункты (POST-guard в `solo_room()` всё ещё не блокирует `"abandoned"`, мёртвая переменная в `_check_and_make_complete`) — в [`TODO.md`](TODO.md), сессия от 2026-09-17.
- **Сессия от 2026-09-18 (ветка `rounds_creating`): доступ участников комнаты к `solo_room`, редирект хоста-не-игрока не в игру, а обратно в лобби.** Пользователь правил код сам, Claude — консультации + ревью каждого диффа (без своих правок кода, кроме документации этим коммитом). Две отдельные правки:
  - **`gameplay/views.py::solo_room()`** — доступ на чтение расширен с одного `series_run.created_by` до `created_by` **или** участника комнаты (`series_run.room.room_players`, если `series_run.room` не `None`) — нужно было, чтобы ссылка «История игр» в `room_detail.html` (ведёт на `gameplay:solo_room` для завершённых/брошенных мультиплеерных прогонов) открывалась не только хосту. При этом `POST`-ветка (создание `GameSession` следующего раунда) осталась ограничена строго `created_by` — иначе рядовой участник комнаты мог бы напрямую дёрнуть URL и создать отдельный `mode="solo"` `GameSession` по текущему мультиплеерному раунду в обход `room_start` (с одним собой как `GameParticipant`), что при завершении продвинуло бы общий `series_run.current_round_index` для всей комнаты в обход реального мультиплеерного прогресса. Заодно расширен и статусный guard POST-ветки — раньше блокировал только `status == "completed"`, теперь `["completed", "abandoned"]` (закрывает давний открытый пункт TODO про `"abandoned"`).
  - **Хост, не являющийся `RoomPlayer` (осознанно допустимая по дизайну лобби роль «модератор»), раньше получал `PermissionDenied` при старте раунда** — старый открытый пункт бэклога (см. ниже, снят из списка). Причина: и HTTP-редирект из `multiplayer/views.py::room_start()` (обе точки — резюме уже идущего раунда и создание нового), и WS-сообщение `{"type": "redirect", ...}` из `multiplayer/consumers.py::RoomConsumer.room_update()` безусловно вели на `gameplay:play`, а там `GameParticipant` для такого хоста не создаётся (`room_start` раздаёт участников только по `room.room_players.all()`). Исправлено симметрично в обоих файлах: `is_player = any(p.user_id == user.id for p in room.room_players.all())` считается один раз и решает, куда вести — `gameplay:play`, если `is_player`, иначе обратно на `multiplayer:room_detail` (там прогресс серии для хоста уже отображается сам, через существующий `_room_status.html` → `gameplay/_series_progress.html`, без доп. правок шаблона). В `room_start()` заодно исправлен prefetch — `room_players__user` вместо `room_players` — это чинит скрытый N+1 на `participant.user` в цикле создания `GameParticipant` (существовал и раньше, не был частью исходной задачи, попутная находка).
  - **Известная не закрытая мелочь (осознанно отложена пользователем)**: на последнем раунде серии, в момент её завершения, страница у хоста обновляется не совсем корректно — деталь не диагностирована, планируется разобрать в одной из следующих сессий.
- `social`, `notifications` из плана ещё не созданы. **`multiplayer` — Этап 3 (мультиплеер) считается завершённым в первой итерации** (ветка `multiplayer_start`, закрывается по итогам сессии от 2026-09-04) — полный цикл проверен вручную в реальном браузере: создание комнаты → приглашение → выбор квиза хостом → готовность игроков → «Начать» (`room_start`) → игра с общим `current_question` на всех участников, **включая live-обновление у ожидающих игроков через собственный WS сессии** (`GameSessionConsumer`, группа `session_<id>`, реализовано 2026-09-04 — раньше это был единственный открытый пункт основного цикла) → завершение → результат со ссылкой «Вернуться в комнату» → новый раунд в той же комнате, при этом комната полностью сбрасывается (`current_quiz=None`, готовность всех игроков снята, лобби узнаёт об этом мгновенно через WS) без старых редиректов. `gameplay` — соло-флоу и мультиплеерная ветка `play()`/`result()` реализованы и обкатаны (см. сессии от 2026-08-07…2026-09-04 ниже); таймер вопроса — и клиентский, и серверный (см. сессию от 2026-08-14/16). Все находки нескольких раундов security-ревью `gameplay/views.py` закрыты (сессии от 2026-08-10/11) — подробности в [`TODO.md`](TODO.md). Все вызовы `_notify_room`/`_notify_session` (оба приложения) обёрнуты в `transaction.on_commit(...)` — закрывает гонку «WS-консьюмер прочитал БД раньше, чем закоммитилась транзакция» (см. сессию от 2026-09-04). **Из основного мультиплеерного цикла в бэклог следующей итерации остаются**: Celery-сторож зависших игроков, доп. UI итоговой таблицы счёта — хост-не-`RoomPlayer` → `PermissionDenied` на `gameplay:play` закрыт сессией от 2026-09-18 выше — ни один из оставшихся не блокирует уже работающий цикл, см. сессию от 2026-09-04 и [`TODO.md`](TODO.md).

## История сессий

Детальный постраничный журнал по сессиям разработки (начиная с сессии от 2026-08-05) вынесен в [`CLAUDE_HISTORY.md`](CLAUDE_HISTORY.md) — этот файл превысил лимит Claude Code на размер `CLAUDE.md` (~150 000 символов, читается целиком в начале каждой сессии). Здесь остаётся только сводка текущего состояния выше; за деталями конкретных решений/багов — в архив.

Этот раздел ("Текущее состояние") стоит обновлять по мере продвижения, чтобы новая сессия быстро понимала, на каком этапе находится разработка. Новые сессионные записи с этого момента добавляются в `CLAUDE_HISTORY.md`, а не сюда.
